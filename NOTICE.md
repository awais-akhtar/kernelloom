# Project history

The original compiler and runtime were extracted from the
`openagent/adaptive_engine` package in OpenAgent commit
`928ac060070e12d1f87f475f2bd09eee29dacf60`.

KernelLoom continues that work as an independent package with its own model
API, serving layer, integrations, local state and command-line tools.
