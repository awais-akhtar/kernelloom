# Origin

The engine was extracted from the `openagent/adaptive_engine` package in
OpenAgent commit `928ac060070e12d1f87f475f2bd09eee29dacf60`.

OpenAgent remains unchanged. This repository provides the same engine boundary
as an independently installable Python package, with a small standalone SQLite
store and command-line interface.
